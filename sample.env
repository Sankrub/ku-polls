# Copy this file to .env and edit the values

# Create a secret key:
#- You can generate a new secret key using Django's built-in tool.
#- Open a terminal/command prompt and navigate to your project's directory.
#- Run the following command to generate a new secret key:
#- python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
#- Copy the generated key (excluding the quotes) and paste it below.
SECRET_KEY = your-secret-key-value-without-quotes

# Set DEBUG to True for development, False for actual use
DEBUG = True

# ALLOWED_HOSTS is a comma-separated list of hosts that can access the app.
# You can use wildcard chars (*) and IP addresses. Use * for any host.
ALLOWED_HOSTS = *.ku.th, localhost, 127.0.0.1, ::1

# Your timezone
TIME_ZONE = Asia/Bangkok
